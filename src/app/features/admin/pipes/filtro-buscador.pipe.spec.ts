import { FiltroBuscadorPipe } from './filtro-buscador.pipe';

describe('FiltroBuscadorPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroBuscadorPipe();
    expect(pipe).toBeTruthy();
  });
});
