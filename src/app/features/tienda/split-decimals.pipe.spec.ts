import { SplitDecimalsPipe } from './split-decimals.pipe';

describe('SplitDecimalsPipe', () => {
  it('create an instance', () => {
    const pipe = new SplitDecimalsPipe();
    expect(pipe).toBeTruthy();
  });
});
