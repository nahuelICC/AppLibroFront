import { NombreGeneroPipe } from './nombre-genero.pipe';

describe('NombreGeneroPipe', () => {
  it('create an instance', () => {
    const pipe = new NombreGeneroPipe();
    expect(pipe).toBeTruthy();
  });
});
